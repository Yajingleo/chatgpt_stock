load("@gazelle//:def.bzl", "gazelle")
load("@rules_python//python:defs.bzl", "py_binary")

# Gazelle target for automatic BUILD file generation
gazelle(
    name = "gazelle",
    prefix = "github.com/yourusername/stock_analysis",
)

# Update BUILD files automatically
gazelle(
    name = "gazelle-update-repos",
    args = [
        "-from_file=go.mod",
        "-to_macro=deps.bzl%go_dependencies",
        "-prune",
    ],
    command = "update-repos",
)

# Python specific Gazelle rules (removed for now due to dependency issues)
# load("@rules_python//gazelle:py_extensions.bzl", "py_gazelle")

# py_gazelle(
#     name = "py_gazelle",
#     srcs = glob(["**/*.py"]),
# )

# Original BUILD content below this line...

# Chat server launcher binary
py_binary(
    name = "launch_chat_server",
    srcs = ["launch_chat_server.py"],
    main = "launch_chat_server.py",
    deps = ["//stock_selection/agent:adk_stock_main_agent"],
    visibility = ["//visibility:public"],
)

# Web chat server binary  
py_binary(
    name = "adk_web_chat_server",
    srcs = ["adk_web_chat_server.py"],
    main = "adk_web_chat_server.py",
    deps = ["//stock_selection/agent:adk_stock_main_agent"],
    visibility = ["//visibility:public"],
)